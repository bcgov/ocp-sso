---
  - hosts: localhost
    gather_facts: no
    vars:
      SSO_DC_NAME: sso-sbox-121
      TARGET_NAMESPACE: devops-sso-sandbox
      HPA_NAME: matt_damon
    tasks:
      - debug: 
          msg: "--------- Scaling SSO and Patroni in order ---------"

      - name: Adding HPA {{ HPA_NAME }} to {{ SSO_DC_NAME }}
        include_tasks: ./tasks/create_or_delete_sso_hpa.yml
        vars:
          _action: create
        when: HPA_NAME != 'matt_damon'     

